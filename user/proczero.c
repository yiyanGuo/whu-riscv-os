#include "user.h"

void _start(void){
    print0();
    while(1);
    // int pid = getpid();
    // for(int i = 0; i < 100000000; i++){
    //     if(i % 10000 == 0) {
    //         putstr("pid = ");
    //         putint(pid);
    //         putstr("\n");
    //     }
    // }

    // exit();
}